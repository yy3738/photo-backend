-- ============================================================
-- 初始化用户 & 用户-角色关联，然后删除 role 冗余列
-- 版本：V0.3
-- 说明：插入测试用户（管理员/摄影师/买家），建立 user_role 关联，
--       最后移除 t_sys_user.role 字段（角色已由关联表管理）
-- ============================================================

-- -----------------------------------------------------------
-- 1. 初始化用户
-- -----------------------------------------------------------
INSERT INTO t_sys_user (id, openid, nickname, avatar, role, points) VALUES
(1, 'oTest_admin_001',        '管理员小张', '', 'admin',        0),
(2, 'oTest_photographer_001', '摄影师老李', '', 'photographer', 500),
(3, 'oTest_buyer_001',        '买家小王',   '', 'buyer',        1000);

-- -----------------------------------------------------------
-- 2. 用户-角色关联
-- -----------------------------------------------------------
INSERT INTO t_sys_user_role (id, user_id, role_id) VALUES
(1, 1, 1),   -- 管理员小张 → admin
(2, 2, 2),   -- 摄影师老李 → photographer
(3, 3, 3);   -- 买家小王   → buyer

-- -----------------------------------------------------------
-- 3. 删除 role 冗余列（角色已由 t_sys_user_role 管理）
-- -----------------------------------------------------------
ALTER TABLE t_sys_user DROP COLUMN role;
